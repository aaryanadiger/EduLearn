"use client";

// We use native CSS cursors in globals.css now for absolutely zero lag,
// which is required by the project specifications to use the macOS cursors properly.
export default function CustomCursor() {
    return null;
}
